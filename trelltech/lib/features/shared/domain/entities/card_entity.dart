class CardEntity {
  final String? id;
  final String? desc;
  final String? name;
  final bool? closed;
  final String ? idList;

  CardEntity(
      {required this.id,
      required this.desc,
      required this.name,
      required this.closed,
      required this.idList});
}
